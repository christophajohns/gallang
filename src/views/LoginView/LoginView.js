import './LoginView.css';
import {
    Button,
} from '../../components';

function LoginView() {
    return (
        <div className="LoginView">
        This is a login view.
            <Button>Login</Button>
        </div>
    );
}

export default LoginView;